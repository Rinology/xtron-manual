# 패치 노트 (Patch Notes)

본 문서는 향후 메인(main) 브랜치에 코드를 병합할 때 참고하기 위해 작성된 변경 사항 기록입니다.

---

## 📅 [2026-09-08] 사이드바 메뉴 빈 폴더 생략 및 직통 링크(Direct Link) 렌더링 추가

### ✨ UX 개선 및 기능 고도화 (UX Improvements & Features)
- **빈 하위 카테고리 폴더(아코디언) 생성 방지 및 직통 연결 (`googleSheetsCMS.jsx`, `Sidebar.jsx`)**
  - 스프레드시트 작성 시 `SubCategoryTitle` 또는 `ChildCategoryTitle` 열을 비워두면 의미 없는 빈 아코디언 폴더가 생성되던 문제를 해결했습니다.
  - 하위 항목이 필요 없는 단일 메뉴(예: 설명서 다운로드)의 경우, 중간 단계(Sub, Child)를 비워두면 사이드바에서 메인 타이틀(Category) 자체가 바로 본문으로 이동하는 '직통 링크(버튼)'로 똑똑하게 렌더링되도록 로직을 개선하여 클릭 뎁스(Depth)를 최소화했습니다.

---
## 📅 [2026-09-08] PWA PDF 런타임 캐싱 설정 추가

### ✨ 신규 기능 (Features)
- **PDF 다운로드 페이지 마크다운(Markdown) 기반 전환 (`downloads.md`, `GuideContent.jsx`)**
  - 기존에 독립된 하드코딩 컴포넌트(`Downloads.jsx`)로 존재하던 다운로드 페이지를 완전히 제거하고, 구글 스프레드시트 CMS에서 다른 문서들처럼 자유롭게 순서와 내용을 제어할 수 있도록 순수 마크다운 파일(`downloads.md`) 기반으로 개편했습니다.
  - **마크다운 커스텀 뷰어 업그레이드**: 마크다운 파일 내에서 `.pdf` 파일 확장자를 가진 링크(예: `[설명서 | 기본 조작 가이드](manual1.pdf)`)를 작성하면, 기존 `Downloads.jsx` 컴포넌트처럼 예쁘고 큼직한 '다운로드 버튼' UI(아이콘, 그림자, 호버 효과 포함)로 자동 변환 렌더링되도록 `a` 태그 렌더러를 고도화했습니다.
  - 다운로드 버튼 클릭 시 `.env`에 등록된 `VITE_R2_PDF_URL` (Cloudflare R2 버킷 주소)이 자동으로 결합되어 비용 효율적인 다운로드가 즉시 수행됩니다.
- **R2 호스팅 환경변수 추가 (`.env`)**
  - `.env`에 `VITE_R2_PDF_URL` 변수를 신설하여 R2 버킷 URL을 쉽게 교체할 수 있도록 확장성을 확보했습니다.

### 🐛 버그 수정 및 안정성 향상 (Bug Fixes & Stability)
- **CMS 스프레드시트 빈 행 쓰레기 데이터 렌더링 버그 수정 (`googleSheetsCMS.jsx`)**
  - 구글 스프레드시트 하단에 실수로 생성된 빈 행(예: 체크박스 값 `FALSE`만 있는 수백 개의 빈 줄)들을 파서가 읽어들여 사이드바 맨 밑에 이름 없는 유령 카테고리(`>`)와 엄청나게 긴 빈 공간을 생성해버리는 버그를 수정했습니다. 이제 ID나 제목이 없는 의미 없는 행은 파서가 자동으로 완전히 무시합니다.
- **탑레벨 아이템(직속 아이템) 렌더링 후 '마법서를 찾을 수 없습니다' 에러 해결 (`GuideContext.jsx`)**
  - 스프레드시트에서 서브카테고리 없이 대분류에 직접 연결된 아이템을 추가했을 때, 사이드바에는 렌더링되나 클릭 시 라우팅 목록(`allGuideItems`)에서 누락되어 오류 페이지로 튕기는 버그를 수정했습니다. 이제 대분류 직속 아이템도 정상적으로 라우팅을 찾아갑니다!
- **CMS 빈 서브카테고리(중분류) 렌더링 버그 수정 (`googleSheetsCMS.jsx`)**
  - 스프레드시트에서 `SubCategoryID`(중분류)를 비워두고 단일 메뉴로 설정할 경우, 시스템이 강제로 이름 없는 빈 중분류 폴더를 생성하여 사이드바에 의미 없는 스크롤과 빈 공간이 발생하는 문제를 해결했습니다.
  - 이제 중분류를 비워두면 자동으로 대분류의 직속 단일 아이템으로 인식되어, 사이드바에서 아코디언 폴더 없이 즉시 클릭 가능한 깔끔한 버튼 형태로 렌더링됩니다.

### ✨ 기능 추가 및 비용 최적화 (Features & Optimization)
- **Vite PWA (Workbox) PDF 런타임 캐싱 적용 (`vite.config.js`)**
  - 서비스 워커를 통해 배포된 PDF 파일(`.pdf`)을 브라우저 로컬 캐시에 저장(`runtimeCaching`)하도록 설정을 추가했습니다. 
  - 사용자가 한 번 다운로드한 PDF 문서를 오프라인 상태에서도 다시 열람할 수 있으며, 불필요한 트래픽 발생을 방지하여 데이터 전송 비용을 최적화했습니다. (최대 10개 파일, 30일 보관)

### 📝 문서화 (Documentation)
- **관리자용 마크다운 가이드 문서 업데이트 (`admin.md`)**
  - 메인 화면 검색창 하단에 노출되는 "자주 찾는 질문" 추천 키워드(태그)를 프론트엔드 코드(`Hero.jsx`)에서 직접 수정하고 관리하는 방법과 주의사항을 7번 항목으로 새롭게 추가했습니다.
---

## 📅 [2026-08-25] 사이드바 아이템 툴팁 추가

### ✨ 기능 추가 및 접근성 향상 (Features & Accessibility)
- **사이드바 메뉴 아이템 툴팁(Title) 추가 (`src/components/Sidebar.jsx`)**
  - 사이드바 메뉴 텍스트가 너무 길어서 말줄임표(`...`)로 잘릴 때, 마우스를 올리면 전체 텍스트 내용이 브라우저 기본 툴팁으로 표시되도록 `<button>` 요소에 `title` 속성을 추가했습니다.

---

## 📅 [2026-08-19] CDN 기본 URL 폴백 및 킥스탠드(타입 A, B) 이미지/로컬 메뉴 보완

### 🐛 버그 수정 및 안정성 향상 (Bug Fixes & Stability)
- **`Ctrl + K` 검색 단축키 한글 입력기(IME) 및 캡스락 호환성 강화 (`src/App.jsx`)**
  - 한글 키보드 입력 상태(IME 조합 중 `e.key === 'ㅏ'`)나 Shift/Caps Lock 조합 시 브라우저 주소창 검색으로 넘어가던 현상을 방지하도록 물리키 `e.code === 'KeyK'` 및 Capture 페이즈 단축키 이벤트를 적용했습니다.
- **S3 키 경로(`/` 유무 및 프로젝트 경로 포함 여부) 자동 정규화 (`src/components/GuideContent.jsx`)**
  - AWS S3 콘솔에서 키를 복사할 때 맨 앞 슬래시(`/`)가 없거나 (`project/xtron-guide/...`), 프로젝트 폴더 경로가 없는 형태 (`parts/...`)로 복사되더라도 자동으로 `/project/xtron-guide/...` 주소를 붙여 CDN(`https://cdn.xtron-guide.kr`)에서 정상 로드되도록 정규화 로직을 강화했습니다.
- **CDN 이미지 경로 폴백 추가 (`src/components/GuideContent.jsx`, `Header.jsx`, `Sidebar.jsx`)**
  - 환경변수 `VITE_CDN_URL`이 설정되어 있지 않은 환경에서도 상대 경로 이미지(`/project/xtron-guide/...`) 및 로고가 정상적으로 CDN(`https://cdn.xtron-guide.kr`) 주소를 바라보도록 기본값(Fallback) 처리를 구현했습니다.
  - 이로 인해 킥스탠드 타입 A(`kickstand-TypeA.md`) 및 타입 B(`kickstand-TypeB.md`)를 포함한 모든 마크다운 내부 이미지 렌더링 미출력 현상을 해결했습니다.

### 📋 가이드 데이터 및 로컬 메뉴 보완 (`src/data/guides.jsx`)
- **로컬 가이드 메뉴에 킥스탠드(타입 A, B) 항목 추가**
  - CMS(구글 스프레드시트) 외 오프라인/로컬 폴백 메뉴 데이터(`guidesData`)에도 `kickstand-TypeA` 및 `kickstand-TypeB` 항목을 추가하여 연결성을 보장했습니다.
- **썬투어 싯포스트 가이드(`src/data/markdown/parts/suntour-seatpost.md`) 이미지 정보 반영**
  - 기존 `basic/`에서 `parts/`로 이동된 `parts/suntour-seatpost.md` 파일에 사용자의 최신 이미지 마크다운 수정본을 즉시 업데이트 반영했습니다.

---

## 📅 [2026-08-18] 부품 관련 마크다운 파일 parts 디렉토리 정리

### 📁 파일 구조 개편 (Refactoring & Organization)
- **부품 관련 MD 파일 `src/data/markdown/parts/` 디렉토리로 이동 및 경로 분리**
  - 기존 `src/data/markdown/basic/`에 위치하던 부품 관련 마크다운 문서 9종을 새로 생성된 `src/data/markdown/parts/` 디렉토리로 이동하여 체계적으로 분리 정리했습니다.
  - 이동된 파일:
    - `pedal.md`
    - `kickstand-TypeA.md`
    - `kickstand-TypeB.md`
    - `battery-braket.md`
    - `Frontrack-TypeC.md`
    - `suntour-seatpost.md`
    - `brake-pad.md`
    - `lcd-display-replacement.md`
    - `keybox-replacement.md`
- **가이드 데이터 참조 경로 업데이트 (`src/data/guides.jsx`)**
  - 이동된 파일(`pedal.md`, `suntour-seatpost.md` 등)의 `markdownFile` 참조 경로를 `parts/...`로 일치하도록 업데이트했습니다.
  - `lcd/`에서 `lcd-manual/`로 변경된 LCD 가이드 마크다운 파일들의 참조 경로(`lcd-manual/...`)를 업데이트했습니다.

---

## 📅 [2026-07-28] 영문 및 한글 폰트 단일화 (페이퍼로지 전면 적용)

### ✨ 기능 추가 및 문서화 (Features & Documentation)
- **페달 장착 가이드(`pedal.md`) 마크다운 포맷팅 전면 개편 및 가독성 개선**
  - 제목 크기를 키우고(`####` -> `###`) 각 단락 앞에 직관적인 이모지(🛠️, 🔍, 🔧, ⚠️, ✅, 🚑, 📊)를 추가하여 시각적 계층 구조를 명확히 했습니다.
  - "손으로 먼저 가결합", "우측은 시계 방향, 좌측은 반시계 방향" 등 헷갈리기 쉽고 치명적인 안전 정보를 `> [!IMPORTANT]`, `> [!WARNING]` 등의 깃허브 스타일 경고 알림 박스로 분리하여 눈에 잘 띄게 강조했습니다.
  - 고객센터 및 전국 대리점 바로가기 버튼 2종을 '문제 해결' 아코디언 내부 하단에 나란히 배치했습니다.

### 💄 스타일 및 최적화 (Styling & Optimization)
- **페달 장착 가이드(`pedal.md`) 및 마크다운 뷰어 Mermaid 다이어그램 렌더링 개선**
  - 페달 조립 순서도(가로형)의 각 노드 텍스트에 줄바꿈(`<br>`)을 적용하여 다이어그램이 가로로 지나치게 길어지거나 세로로 너무 뚱뚱해지지 않도록 황금비율(Aspect Ratio)을 맞췄습니다.
  - 모바일이나 좁은 화면에서 다이어그램이 억지로 작아져 글씨가 뭉개지는 현상을 방지하기 위해, `GuideContent.jsx`의 렌더러에 `minWidth: '600px'` 제한과 가로 스크롤(`overflowX: 'auto'`)을 도입하여 사용자가 스와이프하여 선명하게 볼 수 있도록 개선했습니다.
- **불필요한 구글 폰트(`Outfit`) 제거 (`index.html`, `index.css`)**
  - 페이퍼로지(`Paperozi`) 폰트가 영문과 한글을 모두 완벽하게 지원하므로, 기존에 영어 폰트로 사용되던 `Outfit` 폰트 링크를 `index.html`에서 삭제하여 초기 로딩 속도를 최적화했습니다.
  - `src/index.css`의 `--font-body` 선언부에서 2순위로 지정되어 있던 `'Outfit'`을 제거하여 페이퍼로지 폰트가 앱 전체에 일관되게 단일 적용되도록 수정했습니다.

---

## 📅 [2026-07-24] 보조 브라켓 장착 방법 시각화 추가 및 CMS 필터링 기능 구현

### ✨ 신규 기능 (Features)
- **CMS (스프레드시트) 배포 상태 제어 시스템 고도화 (`googleSheetsCMS.jsx`)**
  - 라이브(메인) 서버와 테스트(브랜치) 서버를 도메인(`xtron-guide.kr`) 기준으로 분리하여 다른 노출 규칙을 적용했습니다.
  - 매뉴얼 가이드 연동은 물론 자가진단 마법사(Wizard) 연동에도 동일하게 적용되었습니다.
  - **[배포 상태별 노출 규칙 표]**
    
    | `Status` 입력값 | 라이브 서버 (Main) | 테스트 서버 (Branch) | 비고 (설명) |
    | :--- | :---: | :---: | :--- |
    | **Deploy** | 🟢 노출 | 🟢 노출 | 정식 라이브 배포 완료 상태 |
    | **BranchDeploy** | ❌ 숨김 | 🟢 노출 | 신규 작업 중, 브랜치에서만 테스트 노출 |
    | **Hold / Delete** | ❌ 숨김 | ❌ 숨김 | 전체 숨김 처리 및 사용 중단 (삭제) 상태 |

### 📝 문서화 (Documentation)
- **자전거 안장 높이 가이드 시각화 및 UI 개선 (`seat-height.md`)**
  - 마크다운 문법을 리스트 형태로 교정하여 불필요한 줄간격을 통일했습니다.
  - 올바른 안장 높이 찾는 법(1~4단계)과 조절 순서(1~5단계)의 직관성을 위해 각각 Mermaid 다이어그램(순서도)을 추가했습니다.
  - 안전 한계선 경고문에는 `> [!CAUTION]`, 조절 팁에는 `> [!TIP]`, 모델 안내에는 `> [!NOTE]`를 적용하여 가시성을 극대화했습니다.
  - 이해를 돕기 위해 각 핵심 단계(올바른 발 위치, 클램프 여닫기, 안전 한계선 확인, 안장 코 정렬)에 추후 실제 사진을 넣을 수 있도록 임시 이미지(Placeholder) 영역을 지정해 두었습니다.
- **관리자용 마크다운 가이드 문서 업데이트 (`admin.md`)**
  - 마크다운 문서 내 HTML 태그(`<br>`, `<span>` 등) 활용 방법과 렌더링 충돌(Mermaid 에러 등) 방지를 위한 2가지 핵심 주의사항을 5번 항목으로 추가했습니다.
- **모든 마크다운(MD) 문서 내 깨진 이미지 일괄 수정 (글로벌 패치)**
  - `handle-height.md`, `pedal.md`, `error-throttle.md`, `kickstand.md` 등 총 8개의 문서에 존재하던 엑스박스(존재하지 않는 로컬 경로 참조) 이미지를 모두 색출했습니다.
  - 해당 이미지들을 각 사진의 설명(Alt Text)이 적힌 `placehold.co` 회색 임시 박스로 일괄 변환하여, 사용자가 시각적으로 불쾌감을 느끼지 않도록 UI를 깔끔하게 정돈했습니다.

---

## 📅 [2026-07-23] 검색 플레이스홀더 텍스트 변경

### 💄 스타일 및 UI 개선 (Styling)
- **검색 텍스트 변경 (`Sidebar.jsx`)**
  - 사이드바가 열려있을 때 표시되는 검색 텍스트를 `"가이드 검색"` → `"검색 Ctrl + K"`로 변경하여 단축키를 안내합니다.

---

## 📅 [2026-07-22] 사이드바 4단계 계층 구조 개편 및 UI/UX 개선

### ✨ 주요 변경 사항 (Features)
- **UI/UX 개선 (UI/UX Improvements)**
  - **마크다운(MD) 뷰어 엔진 대규모 업그레이드 (`feature/upgrade-md` 브랜치)**
    - `rehype-raw` 플러그인을 도입하여 마크다운 내 `<details>`, `<summary>` 등 HTML 태그 렌더링(아코디언 UI)을 지원합니다.
    - `mermaid` 다이어그램 엔진을 자체 내장하여 마크다운 코드 블록(`graph TD` 등)을 깔끔한 SVG 순서도/흐름도로 자동 렌더링합니다.
    - 깃허브 스타일 알림 박스 지원: `> [!NOTE]`, `> [!CAUTION]` 등의 인용문을 감지하여 Lucide React 아이콘과 함께 색상이 적용된 직관적인 디자인 박스로 커스텀 렌더링합니다.
    - 적용 확인용으로 기존 `basic/pedal.md` 문서 하단에 아코디언 및 Mermaid 다이어그램 예제를 임의로 삽입하여 테스트를 마쳤습니다.
- **사이드바 4단계 구조 확장 (`Sidebar.jsx`, `googleSheetsCMS.jsx`)**
  - 기존 3단계(`대분류` > `중분류` > `본문`)로 제한되어 있던 카테고리 구조를 **4단계(`대분류` > `중분류` > `소분류` > `본문`)**로 유연하게 확장했습니다.
  - 구글 스프레드시트에 `ChildCategoryID`와 `ChildCategoryTitle` 열을 추가하여 소분류 데이터를 파싱하도록 파서를 개선했습니다.
  - 소분류가 필요 없는 항목은 빈칸 처리 시 기존처럼 3단계로 동작하도록 하위 호환성을 완벽하게 보장합니다.
  - 검색 및 마법사 기능 호환성을 위해 로컬 가이드 데이터(`guides.jsx`)의 `allGuideItems` 평탄화 로직을 업데이트했습니다.

- **사이드바 컴포넌트 렌더링 최적화 (`Sidebar.jsx`)**
  - 소분류 항목의 열림/닫힘(Accordion) 상태 관리를 위한 `openChildCategories` 로직을 추가했습니다.
  - 토스페이먼츠 스타일의 시각적 계층 분리를 위해, 4단계 최하위 본문 렌더링 시 여백(padding)을 조절했습니다 (`level = 3`).

### 💄 스타일 및 디자인 개선 (Styling)
- **전역 폰트 강제 적용 확인 (`index.css`)**
  - 전체 시스템에 `Paperozi` 폰트가 최우선으로 적용되어 있도록 폰트 선언부를 재점검하고 확인했습니다.


### 📝 문서화 (Documentation)
- **관리자용 마크다운 작성 가이드 문서 추가 (`markdown-guide.md`)**
  - 사이트 내(좌측 메뉴)에서 일반 관리자나 사용자가 바로 참고할 수 있도록 `src/data/markdown/admin/markdown-guide.md` 파일을 생성하여 알림 박스, 아코디언, 다이어그램 등 다채로운 마크다운 작성법 예시를 추가했습니다.
- **CMS 가이드라인 업데이트 (`README.md`)**
  - 마크다운 문서 및 구글 스프레드시트 관리법 가이드의 작성 예시를 프로젝트 컨텍스트에 맞게 **'자전거 AS 매뉴얼(배터리 가이드)'** 관련 예시로 전면 교체했습니다.
  - 새롭게 추가된 선택적 소분류(ChildCategory) 사용 팁을 추가하여 관리자가 혼동 없이 작성할 수 있도록 문서화했습니다.
  - 마크다운 에디터 역할을 하는 일반 유저가 브랜치나 PR 없이 안전하게 동시 작업할 수 있도록 **작업 영역(폴더) 분리**를 통한 깃허브 충돌(Conflict) 방지 팁을 추가했습니다.

---

## 📅 [2026-07-22] 소분류 아이템 연결 버그 수정 및 UI 개선 (2차)

### 🐛 버그 픽스 (Bug Fixes)
- **소분류(ChildCategory) 아이템 "준비되지 않음" 페이지 연결 버그 수정 (`GuideContext.jsx`)**
  - 원격(구글 시트) 데이터 로딩 시 `allGuideItems` 평탄화 로직이 `subCat.items`만 수집하고 `childCategories` 내부의 아이템을 누락하고 있었습니다. 이로 인해 소분류에 속한 아이템을 클릭하면 마크다운 파일이 연결되지 않고 "준비되지 않았습니다" 페이지로 이동하던 문제를 수정했습니다.

### 💄 스타일 및 UI 개선 (Styling)
- **사이드바 트리 구조 가이드라인 추가 (`Sidebar.jsx`)**
  - IDE(VS Code)의 들여쓰기 가이드 스타일로 계층별 좌측에 얇은 수직선(`1px solid #dde3ea`)을 적용하여 소속 관계를 시각적으로 명확하게 표시합니다.
- **소분류 없는 아이템 정렬 순서 변경 (`Sidebar.jsx`)**
  - 중분류 내에서 소분류(ChildCategory)가 먼저 표시되고, 소분류에 속하지 않는 일반 아이템은 **하단에 배치**되도록 렌더링 순서를 변경했습니다.
- **검색 툴팁 변경 (`Sidebar.jsx`)**
  - 검색 버튼 툴팁을 `"가이드 검색"` → `"검색 Ctrl + K"`로 변경하여 단축키를 안내합니다.
